if you have obsurved my previous projects, you would know that I'm not good at using "char" deta type, I've always tried to avoid useing it and use other data types insted
so this project is done to help me learn and practice how to use "char" datatype
initially i looked for a word game to code and the most famous game based on only alphabets is "FLAMES",hence i decided to code that game
concepts such as arrays, loops, conditional statements, and standard library functions to show step-by-step problem solving
The use of functions from the string library like strcpy() and strlen() shows how built-in functions simplify string handling by enabling copying and length calculation without manual iteration.
The "for loop" and "while loop" is used to repeatedly apply a reduction process until a stopping condition is reached,
Finally, a chain of "if-else" conditions is used for decision making based on a single remaining value, showing how multiple outcomes can be handled clearly
